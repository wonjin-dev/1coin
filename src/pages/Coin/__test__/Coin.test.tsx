import {render} from '@testing-library/react';

// const getComponent = (props) => {
//   return <Component {...props} />;
// }

describe('[COIN 페이지 렌더링]', () => {
  let props = {};
  // let component; // = getComponent(props);

  test('render', () => {
    // const rendered = render(component);
    // expect(rendered).toBeTruthy();
  });
});